name: hello-world
description: say Hello World

inputs:
  label-prefix:
    description: prefix of label to filter
    required: false
  token:
    description: GitHub token
    required: true

runs:
  using: 'node16'
  main: 'dist/index.js'
